#include "main.h"
#include <stdio.h>
/**
 * main - entry point
 * @argc: parameter that coutns the total no of arguments passed in from term
 * @argv: a parameter that is an array of the arguments passed in from term
 * Return: Awways 0 (success)
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
