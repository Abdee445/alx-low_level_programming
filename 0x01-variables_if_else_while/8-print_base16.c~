#include <stdio.h>
#include <stdlib.h>
#include <>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int numbers = '0';
	char alphabets = 'a';

	while (numbers <= '9')
	{
		putcar(numbers);
		numbers++;
	}
	while (alphabets <= 'f')
	{
		putchar(alphabets);
	}
	putchar('\n')
	return(0);
}
