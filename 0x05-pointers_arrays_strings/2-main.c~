#include "main.h"
/**
 * _strlen - returns the length of string
 * @s: string
 * return: length of str
 */
int _strlen(char *s)
{
	int lenght = 0;

	while (*(s + length) != '\0')
		length++;
	return (lenght);
}
